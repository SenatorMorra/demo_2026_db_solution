> все решения сгенерированы с использованием ИИ

# ИНСТРУКЦИЯ ПО ВЫПОЛНЕНИЮ МОДУЛЯ 1
## Разработка базы данных средствами СУБД

### Описание задания
Компания занимается продажей обуви. Необходимо разработать базу данных для системы управления товарами и заказами.

---

## ЭТАП 1: ПОДГОТОВКА

### 1.1 Установка MySQL

**Работа выполняется на MySQL!**

**Скачивание и установка:**
1. Скачайте MySQL Community Server: https://dev.mysql.com/downloads/mysql/
2. Версия: **MySQL 8.0** или **MySQL 8.4** (последняя стабильная)
3. Выберите установщик для Windows (MySQL Installer)

**При установке выберите:**
- ✅ MySQL Server
- ✅ MySQL Workbench (обязательно!)
- ✅ MySQL Shell (опционально)
- ✅ Connector/ODBC (опционально)

**Важные настройки при установке:**
- Тип конфигурации: **Development Computer**
- Порт: **3306** (по умолчанию)
- Кодировка: **utf8mb4** (для поддержки кириллицы)
- Пароль root: запомните его!

### 1.2 Дополнительное ПО

**Обязательно:**
- **MySQL Workbench** - для работы с БД и создания ER-диаграмм (устанавливается вместе с MySQL)

**Опционально (альтернативы для ER-диаграмм):**
- Draw.io (diagrams.net) - онлайн редактор
- ERDPlus (erdplus.com) - онлайн инструмент
- dbdiagram.io - онлайн инструмент

**Для работы с данными:**
- Microsoft Excel / LibreOffice Calc (для подготовки CSV)
- Notepad++ / VS Code (текстовые редакторы)

### 1.3 Проверка установки MySQL

**Запуск MySQL Workbench:**
1. Откройте MySQL Workbench
2. Создайте подключение к серверу (если не создано автоматически)
3. Нажмите на подключение (обычно "Local instance MySQL80")
4. Введите пароль root

**Проверка работы через командную строку:**
```bash
# Windows (Command Prompt)
mysql --version

# Должно вывести: mysql Ver 8.0.xx for Win64 on x86_64
```

**Проверка подключения:**
```sql
-- В MySQL Workbench выполните:
SELECT VERSION();
SHOW DATABASES;

-- Должны увидеть список БД и версию MySQL
```

### 1.4 Изучите материалы
1. Откройте файл `Прил_1_ОЗ_КОД 09.02.07-2-2026-М1.docx` - описание предметной области
2. Распакуйте архив `Прил_2_ОЗ_КОД 09.02.07-2-2026-М1.rar` - данные для импорта

---

## ЭТАП 2: АНАЛИЗ ПРЕДМЕТНОЙ ОБЛАСТИ

### 2.1 Основные сущности системы

На основе задания определяем следующие сущности:

#### **1. ПОЛЬЗОВАТЕЛИ (Users)**
- ID пользователя
- Логин
- Пароль
- ФИО
- Роль (Гость, Клиент, Менеджер, Администратор)

#### **2. ТОВАРЫ (Products)**
- ID товара (артикул)
- Наименование
- Категория
- Описание
- Производитель
- Поставщик
- Цена
- Единица измерения
- Количество на складе
- Действующая скидка (%)
- Фото (путь к изображению)

#### **3. КАТЕГОРИИ ТОВАРОВ (Categories)**
- ID категории
- Наименование категории

#### **4. ПРОИЗВОДИТЕЛИ (Manufacturers)**
- ID производителя
- Наименование производителя

#### **5. ПОСТАВЩИКИ (Suppliers)**
- ID поставщика
- Наименование поставщика

#### **6. ЗАКАЗЫ (Orders)**
- ID заказа (артикул)
- Статус заказа
- Адрес пункта выдачи
- Дата заказа
- Дата доставки

#### **7. СОСТАВ ЗАКАЗА (OrderItems)**
- ID записи
- ID заказа
- ID товара
- Количество
- Цена на момент заказа

#### **8. СТАТУСЫ ЗАКАЗОВ (OrderStatuses)**
- ID статуса
- Наименование статуса

#### **9. ПУНКТЫ ВЫДАЧИ (PickupPoints)**
- ID пункта
- Адрес

---

## ЭТАП 3: ПРОЕКТИРОВАНИЕ БАЗЫ ДАННЫХ

### 3.1 Нормализация до 3НФ

**Первая нормальная форма (1НФ):**
- Все атрибуты атомарны
- Нет повторяющихся групп

**Вторая нормальная форма (2НФ):**
- Выполнена 1НФ
- Все неключевые атрибуты зависят от первичного ключа полностью

**Третья нормальная форма (3НФ):**
- Выполнена 2НФ
- Отсутствуют транзитивные зависимости
- Справочные данные вынесены в отдельные таблицы

### 3.2 Схема именования

**Рекомендации:**
- Таблицы: множественное число на английском (Users, Products, Orders)
- Поля: CamelCase или snake_case
- Первичные ключи: ID или TableName_ID
- Внешние ключи: ОписаниеСвязи_ID

---

## ЭТАП 4: СОЗДАНИЕ БАЗЫ ДАННЫХ В MYSQL

### 4.1 SQL-скрипт создания БД

**Выполнение скрипта в MySQL Workbench:**
1. Откройте MySQL Workbench
2. Подключитесь к серверу
3. File → New Query Tab (или Ctrl+T)
4. Скопируйте и вставьте скрипт ниже
5. Execute (молния) или Ctrl+Shift+Enter

**SQL-скрипт для MySQL:**

```sql
-- ============================================
-- СОЗДАНИЕ БАЗЫ ДАННЫХ
-- ============================================
DROP DATABASE IF EXISTS ShoeShop;
CREATE DATABASE ShoeShop CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE ShoeShop;

-- ============================================
-- ТАБЛИЦА: РОЛИ ПОЛЬЗОВАТЕЛЕЙ
-- ============================================
CREATE TABLE Roles (
    Role_ID INT AUTO_INCREMENT PRIMARY KEY,
    RoleName VARCHAR(50) NOT NULL UNIQUE,
    CONSTRAINT CHK_RoleName CHECK (RoleName IN ('Гость', 'Клиент', 'Менеджер', 'Администратор'))
);

-- ============================================
-- ТАБЛИЦА: ПОЛЬЗОВАТЕЛИ
-- ============================================
CREATE TABLE Users (
    User_ID INT AUTO_INCREMENT PRIMARY KEY,
    Login VARCHAR(50) NOT NULL UNIQUE,
    Password VARCHAR(255) NOT NULL,
    LastName VARCHAR(50) NOT NULL,
    FirstName VARCHAR(50) NOT NULL,
    MiddleName VARCHAR(50),
    Role_ID INT NOT NULL,
    CONSTRAINT FK_Users_Roles FOREIGN KEY (Role_ID)
        REFERENCES Roles(Role_ID) ON DELETE RESTRICT ON UPDATE CASCADE
);

-- ============================================
-- ТАБЛИЦА: КАТЕГОРИИ ТОВАРОВ
-- ============================================
CREATE TABLE Categories (
    Category_ID INT AUTO_INCREMENT PRIMARY KEY,
    CategoryName VARCHAR(100) NOT NULL UNIQUE
);

-- ============================================
-- ТАБЛИЦА: ПРОИЗВОДИТЕЛИ
-- ============================================
CREATE TABLE Manufacturers (
    Manufacturer_ID INT AUTO_INCREMENT PRIMARY KEY,
    ManufacturerName VARCHAR(100) NOT NULL UNIQUE
);

-- ============================================
-- ТАБЛИЦА: ПОСТАВЩИКИ
-- ============================================
CREATE TABLE Suppliers (
    Supplier_ID INT AUTO_INCREMENT PRIMARY KEY,
    SupplierName VARCHAR(100) NOT NULL UNIQUE
);

-- ============================================
-- ТАБЛИЦА: ЕДИНИЦЫ ИЗМЕРЕНИЯ
-- ============================================
CREATE TABLE Units (
    Unit_ID INT AUTO_INCREMENT PRIMARY KEY,
    UnitName VARCHAR(20) NOT NULL UNIQUE
);

-- ============================================
-- ТАБЛИЦА: ТОВАРЫ
-- ============================================
CREATE TABLE Products (
    Product_ID INT AUTO_INCREMENT PRIMARY KEY,
    ProductName VARCHAR(200) NOT NULL,
    Category_ID INT NOT NULL,
    Description TEXT,
    Manufacturer_ID INT NOT NULL,
    Supplier_ID INT NOT NULL,
    Price DECIMAL(10, 2) NOT NULL CHECK (Price >= 0),
    Unit_ID INT NOT NULL,
    StockQuantity INT NOT NULL DEFAULT 0 CHECK (StockQuantity >= 0),
    DiscountPercent DECIMAL(5, 2) DEFAULT 0 CHECK (DiscountPercent >= 0 AND DiscountPercent <= 100),
    PhotoPath VARCHAR(255),
    CONSTRAINT FK_Products_Categories FOREIGN KEY (Category_ID)
        REFERENCES Categories(Category_ID) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT FK_Products_Manufacturers FOREIGN KEY (Manufacturer_ID)
        REFERENCES Manufacturers(Manufacturer_ID) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT FK_Products_Suppliers FOREIGN KEY (Supplier_ID)
        REFERENCES Suppliers(Supplier_ID) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT FK_Products_Units FOREIGN KEY (Unit_ID)
        REFERENCES Units(Unit_ID) ON DELETE RESTRICT ON UPDATE CASCADE
);

-- ============================================
-- ТАБЛИЦА: ПУНКТЫ ВЫДАЧИ
-- ============================================
CREATE TABLE PickupPoints (
    PickupPoint_ID INT AUTO_INCREMENT PRIMARY KEY,
    Address VARCHAR(255) NOT NULL UNIQUE
);

-- ============================================
-- ТАБЛИЦА: СТАТУСЫ ЗАКАЗОВ
-- ============================================
CREATE TABLE OrderStatuses (
    Status_ID INT AUTO_INCREMENT PRIMARY KEY,
    StatusName VARCHAR(50) NOT NULL UNIQUE
);

-- ============================================
-- ТАБЛИЦА: ЗАКАЗЫ
-- ============================================
CREATE TABLE Orders (
    Order_ID INT AUTO_INCREMENT PRIMARY KEY,
    OrderArticle VARCHAR(50) UNIQUE,
    Status_ID INT NOT NULL,
    PickupPoint_ID INT NOT NULL,
    OrderDate DATE NOT NULL,
    DeliveryDate DATE,
    CONSTRAINT FK_Orders_Statuses FOREIGN KEY (Status_ID)
        REFERENCES OrderStatuses(Status_ID) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT FK_Orders_PickupPoints FOREIGN KEY (PickupPoint_ID)
        REFERENCES PickupPoints(PickupPoint_ID) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT CHK_DeliveryDate CHECK (DeliveryDate IS NULL OR DeliveryDate >= OrderDate)
);

-- ============================================
-- ТАБЛИЦА: СОСТАВ ЗАКАЗА
-- ============================================
CREATE TABLE OrderItems (
    OrderItem_ID INT AUTO_INCREMENT PRIMARY KEY,
    Order_ID INT NOT NULL,
    Product_ID INT NOT NULL,
    Quantity INT NOT NULL CHECK (Quantity > 0),
    PriceAtOrder DECIMAL(10, 2) NOT NULL CHECK (PriceAtOrder >= 0),
    CONSTRAINT FK_OrderItems_Orders FOREIGN KEY (Order_ID)
        REFERENCES Orders(Order_ID) ON DELETE CASCADE ON UPDATE CASCADE,
    CONSTRAINT FK_OrderItems_Products FOREIGN KEY (Product_ID)
        REFERENCES Products(Product_ID) ON DELETE RESTRICT ON UPDATE CASCADE
);

-- ============================================
-- ИНДЕКСЫ ДЛЯ ОПТИМИЗАЦИИ
-- ============================================
CREATE INDEX IDX_Products_Category ON Products(Category_ID);
CREATE INDEX IDX_Products_Supplier ON Products(Supplier_ID);
CREATE INDEX IDX_Products_Name ON Products(ProductName);
CREATE INDEX IDX_Orders_Date ON Orders(OrderDate);
CREATE INDEX IDX_OrderItems_Order ON OrderItems(Order_ID);
```

### 4.2 Заполнение справочных данных

```sql
-- Роли
INSERT INTO Roles (RoleName) VALUES
('Гость'), ('Клиент'), ('Менеджер'), ('Администратор');

-- Единицы измерения
INSERT INTO Units (UnitName) VALUES
('шт'), ('пара'), ('компл'), ('уп');

-- Статусы заказов
INSERT INTO OrderStatuses (StatusName) VALUES
('Новый'), ('В обработке'), ('Готов к выдаче'), ('Выдан'), ('Отменен');

-- Тестовые пользователи (пароли в открытом виде - для учебных целей)
INSERT INTO Users (Login, Password, LastName, FirstName, MiddleName, Role_ID) VALUES
('admin', 'admin123', 'Администратор', 'Главный', NULL, 4),
('manager', 'manager123', 'Иванов', 'Иван', 'Иванович', 3),
('client', 'client123', 'Петров', 'Петр', 'Петрович', 2),
('guest', 'guest', 'Гость', 'Система', NULL, 1);
```

---

## ЭТАП 5: ИМПОРТ ДАННЫХ

### 5.1 Подготовка данных из архива

1. Распакуйте файл `Прил_2_ОЗ_КОД 09.02.07-2-2026-М1.rar`
2. Изучите структуру файлов (обычно это CSV или Excel)
3. Проверьте соответствие полей вашей структуре БД

### 5.2 Импорт через SQL

**Пример импорта из CSV:**

```sql
-- Импорт категорий
LOAD DATA INFILE '/path/to/categories_import.csv'
INTO TABLE Categories
FIELDS TERMINATED BY ';'
ENCLOSED BY '"'
LINES TERMINATED BY '\n'
IGNORE 1 ROWS
(CategoryName);

-- Импорт производителей
LOAD DATA INFILE '/path/to/manufacturers_import.csv'
INTO TABLE Manufacturers
FIELDS TERMINATED BY ';'
ENCLOSED BY '"'
LINES TERMINATED BY '\n'
IGNORE 1 ROWS
(ManufacturerName);

-- Аналогично для других таблиц
```

### 5.3 Импорт через MySQL Workbench (рекомендуется)

**Способ 1: Table Data Import Wizard**
1. В MySQL Workbench откройте вашу БД ShoeShop
2. Правой кнопкой на нужной таблице → **Table Data Import Wizard**
3. Нажмите **Browse** → выберите CSV файл
4. Настройте параметры:
   - Field separator: `;` (точка с запятой) или `,` (запятая)
   - Line separator: `\n` (перевод строки)
   - Encoding: **UTF-8**
5. Сопоставьте столбцы CSV с полями таблицы
6. Нажмите **Next** → **Next** → **Finish**

**Способ 2: Через Query Editor**
```sql
-- Загрузка данных из локального файла
LOAD DATA LOCAL INFILE 'C:/path/to/file.csv'
INTO TABLE Categories
CHARACTER SET utf8mb4
FIELDS TERMINATED BY ';'
ENCLOSED BY '"'
LINES TERMINATED BY '\n'
IGNORE 1 ROWS
(CategoryName);
```

**Важно для Windows:**
- Используйте прямые слэши `/` в путях, а не обратные `\`
- Или используйте двойные обратные слэши `\\`
- Пример: `C:/Users/danii/Desktop/data.csv`

---

## ЭТАП 6: СОЗДАНИЕ ER-ДИАГРАММЫ

### 6.1 Элементы диаграммы

**Обязательные элементы:**
- ✅ Все таблицы
- ✅ Первичные ключи (PK)
- ✅ Внешние ключи (FK)
- ✅ Связи между таблицами
- ✅ Атрибуты (поля) таблиц
- ⚠️ Типы данных (можно опустить на этом этапе)

### 6.2 Типы связей

- **Users → Roles**: Many-to-One (N:1)
- **Products → Categories**: Many-to-One (N:1)
- **Products → Manufacturers**: Many-to-One (N:1)
- **Products → Suppliers**: Many-to-One (N:1)
- **Orders → OrderStatuses**: Many-to-One (N:1)
- **Orders → PickupPoints**: Many-to-One (N:1)
- **OrderItems → Orders**: Many-to-One (N:1)
- **OrderItems → Products**: Many-to-One (N:1)

### 6.3 Создание диаграммы

**РЕКОМЕНДУЕМЫЙ СПОСОБ: MySQL Workbench (автоматическая генерация)**

1. **Database → Reverse Engineer** (или Ctrl+R)
2. **Stored Connection:** выберите ваше подключение
3. **Next** → введите пароль
4. **Next** → выберите схему **ShoeShop**
5. **Next** → отметьте все таблицы (галочки)
6. **Execute** → дождитесь завершения
7. **Next** → **Close**

**Настройка диаграммы:**
1. На вкладке **EER Diagram** увидите все таблицы
2. Расположите таблицы удобно (перетаскиванием)
3. **Model → Model Options** → вкладка **Diagram**:
   - ✅ Show Column Types
   - ✅ Show Schema Name
4. **File → Export → Export as PNG** (для предпросмотра)
5. **File → Export → Export as PDF** ✅ (сдавать в PDF!)

**Настройки экспорта в PDF:**
- Ориентация: **Landscape** (альбомная)
- Paper Size: **A4** или **A3** (если не влезает)
- Zoom: **100%** или подберите, чтобы всё влезло

**Альтернатива 1: Draw.io (вручную)**
```
1. Перейдите на app.diagrams.net
2. Создайте новую диаграмму
3. Используйте фигуры Entity Relationship
4. Добавьте таблицы вручную
5. Настройте связи
6. File → Export as → PDF
```

**Альтернатива 2: ERDPlus (онлайн)**
```
1. Перейдите на erdplus.com
2. Создайте Relational Schema
3. Добавьте таблицы и связи
4. Export → PDF
```

### 6.4 Требования к оформлению

- Формат: **PDF**
- Ориентация: горизонтальная (landscape)
- Масштаб: все должно быть читаемо
- Название: **ER_Diagram_Module1.pdf**

---

## ЭТАП 7: СОХРАНЕНИЕ РЕЗУЛЬТАТОВ

### 7.1 Создание SQL-скрипта

**Полный скрипт БД:**

```sql
-- Экспорт через mysqldump (в командной строке)
mysqldump -u root -p ShoeShop > ShoeShop_Full_Script.sql

-- Или экспорт только структуры
mysqldump -u root -p --no-data ShoeShop > ShoeShop_Structure.sql

-- Экспорт только данных
mysqldump -u root -p --no-create-info ShoeShop > ShoeShop_Data.sql
```

**Через MySQL Workbench (графический интерфейс):**
1. **Server → Data Export**
2. Выберите схему **ShoeShop** (галочка)
3. **Export Options:**
   - ☑ Export to Self-Contained File
   - Укажите путь: `C:/Users/ВашеИмя/Desktop/ShoeShop_Full_Script.sql`
   - ☑ Include Create Schema
   - ☑ Create Dump in a Single Transaction (для целостности)
4. **Start Export**

**Альтернатива - через командную строку MySQL:**
```bash
# Windows (Command Prompt)
cd "C:\Program Files\MySQL\MySQL Server 8.0\bin"

# Полный экспорт с данными
mysqldump -u root -p --databases ShoeShop > ShoeShop_Full_Script.sql

# Только структура (без данных)
mysqldump -u root -p --no-data ShoeShop > ShoeShop_Structure.sql
```

### 7.2 Структура сдаваемых файлов

```
Модуль_1_Результаты/
├── ER_Diagram_Module1.pdf          # ER-диаграмма
├── ShoeShop_Full_Script.sql        # Полный скрипт БД
├── README.txt                       # Пояснения
└── screenshots/                     # Скриншоты (опционально)
    ├── database_structure.png
    └── sample_data.png
```

---

## ЭТАП 8: ПРОВЕРКА РАБОТЫ

### 8.1 Контрольные запросы

```sql
-- Проверка структуры
SHOW TABLES;
DESCRIBE Products;
DESCRIBE Orders;

-- Проверка данных
SELECT COUNT(*) FROM Products;
SELECT COUNT(*) FROM Categories;
SELECT COUNT(*) FROM Orders;

-- Проверка связей
SELECT
    p.ProductName,
    c.CategoryName,
    m.ManufacturerName,
    s.SupplierName,
    p.Price,
    p.StockQuantity
FROM Products p
JOIN Categories c ON p.Category_ID = c.Category_ID
JOIN Manufacturers m ON p.Manufacturer_ID = m.Manufacturer_ID
JOIN Suppliers s ON p.Supplier_ID = s.Supplier_ID
LIMIT 10;

-- Проверка ссылочной целостности (не должно быть записей)
SELECT * FROM Products WHERE Category_ID NOT IN (SELECT Category_ID FROM Categories);
SELECT * FROM Orders WHERE Status_ID NOT IN (SELECT Status_ID FROM OrderStatuses);
```

### 8.2 Проверка ограничений

```sql
-- Попытка вставить отрицательную цену (должна вызвать ошибку)
INSERT INTO Products (ProductName, Category_ID, Manufacturer_ID, Supplier_ID, Price, Unit_ID)
VALUES ('Test', 1, 1, 1, -100, 1);

-- Попытка удалить категорию, используемую в товарах (должна вызвать ошибку)
DELETE FROM Categories WHERE Category_ID = 1;
```

---

## ЭТАП 9: ОФОРМЛЕНИЕ ДОКУМЕНТАЦИИ

### 9.1 README.txt

```text
МОДУЛЬ 1: РАЗРАБОТКА БАЗЫ ДАННЫХ СРЕДСТВАМИ СУБД
=================================================

Автор: [Ваше ФИО]
Дата: [Дата создания]
СУБД: MySQL 8.0 / PostgreSQL 14 / и т.д.

ОПИСАНИЕ СИСТЕМЫ:
База данных для управления магазином обуви.

СТРУКТУРА БД:
- 9 таблиц в 3НФ
- Обеспечена ссылочная целостность
- Все связи реализованы через внешние ключи

СОСТАВ ФАЙЛОВ:
1. ER_Diagram_Module1.pdf - ER-диаграмма базы данных
2. ShoeShop_Full_Script.sql - SQL-скрипт создания и заполнения БД
3. README.txt - данный файл

ИНСТРУКЦИЯ ПО РАЗВЕРТЫВАНИЮ:
1. Установите MySQL Server
2. Запустите SQL-скрипт: mysql -u root -p < ShoeShop_Full_Script.sql
3. Проверьте создание БД: SHOW DATABASES;
4. Используйте БД: USE ShoeShop;

ТЕСТОВЫЕ УЧЕТНЫЕ ЗАПИСИ:
- Администратор: admin / admin123
- Менеджер: manager / manager123
- Клиент: client / client123

ОСОБЕННОСТИ РЕАЛИЗАЦИИ:
- Использована кодировка UTF-8 для поддержки кириллицы
- Цены хранятся с точностью до копеек (DECIMAL(10,2))
- Добавлены проверочные ограничения (CHECK constraints)
- Создан индексы для оптимизации поиска

ПРИМЕЧАНИЯ:
- В production версии пароли должны хешироваться
- Рекомендуется настроить резервное копирование
```

---

## КОНТРОЛЬНЫЙ ЧЕКЛИСТ

### ✅ Обязательные требования

- [ ] База данных создана в выбранной СУБД
- [ ] Реализована 3 нормальная форма
- [ ] Обеспечена ссылочная целостность (FK)
- [ ] Созданы первичные ключи для всех таблиц
- [ ] Согласованная схема именования
- [ ] ER-диаграмма сохранена в формате PDF
- [ ] ER-диаграмма содержит таблицы, связи, атрибуты и ключи
- [ ] Данные из файлов импортированы
- [ ] Создан SQL-скрипт базы данных
- [ ] Скрипт воспроизводим (можно развернуть БД заново)

### ✅ Дополнительная проверка

- [ ] Отсутствуют дублирующиеся данные
- [ ] Все обязательные поля помечены NOT NULL
- [ ] Числовые поля имеют проверки на диапазон
- [ ] Даты имеют логические ограничения
- [ ] Таблицы содержат тестовые данные
- [ ] SQL-скрипт выполняется без ошибок
- [ ] ER-диаграмма читабельна и структурирована

---

## КРИТЕРИИ ОЦЕНИВАНИЯ

### Максимум: 10.00 баллов

**1. Разработка объектов БД (6.00 баллов):**
- Корректная структура таблиц - 2.00 б.
- Правильные типы данных - 1.00 б.
- Первичные ключи - 1.00 б.
- Согласованное именование - 1.00 б.
- ER-диаграмма - 1.00 б.

**2. Реализация БД в СУБД (4.00 балла):**
- Внешние ключи и связи - 1.50 б.
- Ссылочная целостность - 1.00 б.
- Импорт данных - 1.00 б.
- SQL-скрипт - 0.50 б.

---

## ЧАСТО ВСТРЕЧАЮЩИЕСЯ ОШИБКИ

### ❌ Типичные ошибки

1. **Нарушение 3НФ** - хранение повторяющихся данных
2. **Отсутствие внешних ключей** - нет ссылочной целостности
3. **Неправильные типы данных** - использование VARCHAR для чисел
4. **Отсутствие ограничений** - можно вставить отрицательную цену
5. **Кириллица не отображается** - неправильная кодировка
6. **Циклические зависимости** - невозможно импортировать данные
7. **ER-диаграмма нечитаема** - слишком мелкий шрифт в PDF

### ✅ Решения

1. Выносите справочники в отдельные таблицы
2. Всегда создавайте FOREIGN KEY constraints
3. Числа - INT/DECIMAL, даты - DATE/DATETIME, текст - VARCHAR/TEXT
4. Используйте CHECK constraints
5. UTF-8 / utf8mb4 для MySQL
6. Сначала заполняйте справочники, потом основные таблицы
7. Экспортируйте в высоком разрешении, используйте A3 формат

---

## ПОЛЕЗНЫЕ РЕСУРСЫ

### Документация
- MySQL: https://dev.mysql.com/doc/
- PostgreSQL: https://www.postgresql.org/docs/
- SQL Tutorial: https://www.w3schools.com/sql/

### Инструменты
- MySQL Workbench: https://www.mysql.com/products/workbench/
- Draw.io: https://app.diagrams.net/
- ERD Plus: https://erdplus.com/

### Нормализация БД
- Теория нормальных форм
- Примеры нормализации
- Антипаттерны проектирования БД

---

## ПОДДЕРЖКА

При возникновении вопросов:
1. Перечитайте задание в PDF (страницы 28-29)
2. Проверьте соответствие 3НФ
3. Убедитесь, что все FK созданы
4. Проверьте импорт данных
5. Протестируйте SQL-скрипт на чистой БД

**Удачи в выполнении модуля!** 🎓
